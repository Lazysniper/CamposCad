<!DOCTYPE html>
<html lang="pt-PT">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Iniciar sessão</title>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            colors: {
              brand: '#345d9a',
              brandLight: '#5b86d6',
              brandHover: '#2b4f7a',
              canvas: '#fbfbfc',
              surface: '#f1f3f5',
              textDark: '#0b1220',
              textMuted: '#556575',
              borderSoft: '#e6e9ed',
            },
          },
        },
      };
    </script>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="flex min-h-screen items-center justify-center bg-canvas px-6 text-textDark">
    <div class="w-full max-w-sm space-y-6">
      <div class="rounded-2xl border border-borderSoft bg-white p-8 shadow-lg">
        <header class="text-center space-y-2">
          <p class="text-xs font-semibold uppercase tracking-[0.3em] text-brand">
            Campos Cadilhe
          </p>
          <h1 class="text-2xl font-semibold text-textDark">Área de Administração</h1>
          <p class="text-sm text-textMuted">
            Introduza as credenciais para aceder à gestão de códigos postais.
          </p>
        </header>

        <% if (error) { %>
        <div class="mt-6 rounded-xl border border-rose-200 bg-[#fff1f3] px-4 py-3 text-sm text-rose-700">
          <%= error %>
        </div>
        <% } %>

        <form action="/login" method="POST" class="mt-6 space-y-5">
          <input type="hidden" name="next" value="<%= next %>" />

          <div>
            <label for="username" class="block text-sm font-semibold text-textMuted">Utilizador</label>
            <input
              id="username"
              name="username"
              type="text"
              required
              autofocus
              class="mt-1 w-full rounded-lg border border-borderSoft bg-white px-3 py-3 text-textDark shadow-sm focus:outline-none focus:ring-2 focus:ring-brandLight focus:border-brandLight"
            />
          </div>

          <div>
            <label for="password" class="block text-sm font-semibold text-textMuted">Palavra-passe</label>
            <input
              id="password"
              name="password"
              type="password"
              required
              class="mt-1 w-full rounded-lg border border-borderSoft bg-white px-3 py-3 text-textDark shadow-sm focus:outline-none focus:ring-2 focus:ring-brandLight focus:border-brandLight"
            />
          </div>

          <button
            type="submit"
            class="w-full rounded-lg bg-brand px-4 py-3 text-white font-semibold shadow-sm transition hover:bg-brandHover"
          >
            Entrar
          </button>
        </form>
      </div>

      <p class="text-center text-xs text-textMuted">
        Acesso reservado a administradores. Contacte o suporte se não souber as credenciais.
      </p>
    </div>
  </body>
</html>

